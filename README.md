# QAM Modulation & Demodulation

## Задание

Написать на языке С++ класс выполняющий функциональность модулятора QAM (QPSK, QAM16, QAM64)

Написать на языке С++ класс выполняющий функциональность добавления гауссовского шума к созвездию QAM

Написать на языке С++ класс выполняющий функциональность демодулятора QAM (QPSK, QAM16, QAM64)

Написать последовательный вызов 1-3 для случайной последовательности бит для разных значений дисперсия шума
Построить график зависимости вероятности ошибки на бит от дисперсии шума

## Структура проекта 

|── qam_mod.cpp &emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp; # Основной код (модулятор, демодулятор, AWGN, GUI)

|── CMakeLists.txt &emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp; # Файл сборки CMake

|── third_party/  &emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp; # Внешние библиотеки для отрисовки графиков

|── plot.py  &emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp; # Скрипт для построения графика BER по файлу ber_results.csv


|── ber_results.csv      &emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp; # Результаты BER (генерируется при запуске)

── image           &emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp; # Графики




-----------


# Реализация

### 1. Модулятор 

`QAM_gray`:

- Принимает вектор битов и тип QAM (4, 16, 64)
- группирует биты в I и Q компоненты по 2 (для QPSK), по 4 (для 16‑QAM) или по 6 (для 64‑QAM)
- Преобразует двоичные числа I и Q в код Грея:  `gray = value ^ (value >> 1)`
- Вычисляет уровни(амплитуду)

**Код Грея** — это способ представления чисел, где соседние значения отличаются только в одном бите.

В QAM созвездии ближайшие друг к другу символы отличаются малым сдвигом по фазе/амплитуде – именно такие ошибки наиболее вероятны при шуме. Если эти ближайшие символы закодировать с помощью кода Грея, то при ошибочном приёме соседнего символа исказится только один бит, а не несколько. Это сильно снижает BER (Bit Error Rate) при том же SNR, потому что меньше битов портится на одну ошибку символа.
  
  ### 2. Канал AWGN

Функция AWGN добавляет к каждому символу случайный комплексный гауссовский шум с нулевым средним и заданной дисперсией σ². Чем больше дисперсия, тем сильнее размазаны точки на созвездии. Реализована через std::normal_distribution<float> и статический генератор std::mt19937.


  ### 3. Демодулятор

  Функция QAM_demapper выполняет обратные преобразования:

- Денормировка символа 
- Округление до ближайшего целого уровня
- Преобразование уровня в код Грея
- Обратное преобразование Грея → двоичное число по таблице gray2bin[ k-1 ][ gI ]
- Извлечение битов
    
  ### 4. Расчёт BER и сохранение данных

Программа автоматически прогоняет один и тот же набор битов через 21 уровень шума – от очень слабого (дисперсия 0.01) до заметного (дисперсия 1.0). Для каждого уровня демодулируются символы, сравниваются полученные биты с исходными и считается доля ошибок (BER).
Все результаты сохраняются в файл ber_results.csv. Запускается графический интерфейс с передачей всех рассчитанных данных
  
  ### 5. Графический интерфейс (GUI)
Интерфейс написан с помощью библиотек ImGui и ImPlot поверх SDL2 и OpenGL.

- Вкладка «Constellations»
    Здесь можно выбрать дисперсию шума – для этого есть блок кнопок с числами от 0.01 до 1.0. Слева отрисовывается исходное (чистое) созвездие. Справа – созвездие с добавленным шумом для выбранной дисперсии.
- Вкладка «BER»
    Здесь строится график зависимости BER от дисперсии.  График в линейном масштабе, позволяет оценить, насколько быстро растут ошибки при увеличении шума.

Интерфейс работает в полноэкранном режиме. Чтобы выйти, достаточно нажать Escape.
Тип модуляции (4, 16 или 64) выбирается один раз при запуске программы в консоли. Для его смены нужно перезапустить программу.

## Компиляция и запуск
Сборка проекта через CMake

```bash
mkdir build && cd build
cmake ..
make
./qam_mod
```

После запуска предоставится выбор модуляции.

